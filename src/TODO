==================
 01/03
==================

MONITOR:
  - passare a nuova grafica per le license
  - inserire anche versione della console

EVIDENCE:
  - implementare i filtri

==================
 xx/03
==================

BASIC:
  - Disabilitare Accessed files
  - Modificare il testo di keylog e calls

CONFIG:
  - tasto delete per gli elementi
  - connection in mobile non deve avere il pannello di config
  - se salvo la config di un agente, devo tornare alla lista delle config

TASK:
  - a volte il cancellamento fallisce (il task viene rimosso ma il renderer resta appeso)

FRONTEND:
  - validare la creazione di un anon senza ip
 
BUILD:
  - aggiungere tipo di documento in exploit (alor)

AGENTS:
  - visualizzare informazioni estese degli agent nella lista # ident, (uninstalled, istanza, version, last sync time)
  - nella lista delle config non fa refresh (bisogna riscatenare la show)
  - upgrade per gli agent (alor)
  - chiudere un agent
  - info section for agent
  - upload section for agent
  - download section for agent

DASHBOARD:
	- jump-link verso le evidence
	- jump-link verso target dentro operations (con aggiunta recent)
  - sort del dashboard (se complesso, lo rimuoviamo)
  - quando i counter sono 0 non visualizzare il counter
  - icone degli agent devono essere corrette
  - target in operation on funziona

FRONTEND:
  - lista dei file nelle public dei collector (alor)

OPERATIONS:  
  - separare la view delle icone da quella tabellare e riabilitare il sorting sulle tabelle
  - il sort di "all agents" deve ordinare le factory vicine ai propri agenti (possibilmente anche dentro target)
  - pulsante add to dashboard a operation, target, agent

MISC:
  - rimuovere dipendenza da AlertPupUp in UserManager