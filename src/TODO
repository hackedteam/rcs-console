SEARCH:
  - aggiornare la lista a fronte di un rename di un oggetto
  - agganciare all'evento AGENT, TARGET, OPERATION

DASHBOARD:
  - ordinare per sync gli elementi
  - rimuovere da dashboard gli elementi quando si cancellano
  - Manina su dashboard, 1 click, mouseove su icone (tipo system)  
  - refresh subito all'entrata
  - le scrollbar non devono andare sopra gli elementi

CONFIG:
  - caption dinamica (se non specificata)
  - icona conference
  - afterinst e timer loop non hanno end (rimuovere pallino)
  - window non ha repeat e stop (rimuovere pallini)
  - winevent non ha repeat e stop (rimuovere pallini)
  - scroll orizzontale per le frecce (quando si tirano)
  - warning se si esce senza salvare la conf
  - edit delle subaction in orizzontale (se possibile)
  - warning quando si fa "back to basic"
  - Far sparire il selettore delle azioni dopo selezione
  - selezione del modulo (si edita con "edit" o con doppioclick). quando selezionato si comporta come action (mostra solo le azioni a lui collegate)
  - controllare il componente ON / OFF per gli angoli smussati
  - back to basic non deve comparire se gia' in basic

ALERTING:
  - jump-link alle evidence  

OPERATIONS:
  - separare la view delle icone da quella tabellare e riabilitare il sorting sulle tabelle
  - il sort di "all agents" deve ordinare le factory vicine ai propri agenti (possibilmente anche dentro target)
  - pulsante add to dashboard a operation, target, agent
  - rimuovere evidence e fs dalla visualizzazione a griglia, ed evidenziare le factory
  - tasto "up one level" e rendere coerente la navigazione (ogni click un nuovo elemento nel breadcrump, es: config list, config edit, etc). + glow
  - upgrade agent dentro agent deve usare statemanager e non selected object (tipo infopanel)
  - infopanel scazza se si fa jump da dashboard
  - se scelgo "all agents" non compare la actionbar
  - togliere new target da "all target"
  - infopanel non deve uscire dallo schermo (controllare con le localizzazioni)
  - campo "uninstalled" per gli agenti "closed"
  - resettare i filtri quando cambia il breadcrumb (altrimenti togliere i filtri quando si entra in evidence)
  - add to alert (sync e instance)
  
INJECTION:
  - lista agent solo del target selezionato
  
EVIDENCE:
  - si spacca il breadcrumb alla refresh in evidence
  - filtri automatici dal summary (glow + click)  -> time: all type: quello selezionato
  - sistemare tutti i renderer per le evidence
  - togliere process da file di tipo "capture"
  - il campo note in details lo facciamo grande tutta l'altezza
  - X in alto a destra per chiudere i details
  - export di evidence selezionate
  - file open e capture diversi (con program e tipo di accesso)
  - content e' html se la chat e' facebook

ACCOUNTING:
  - menu dei gruppi non deve uscire dallo schermo (edit utente)
  
GENERAL:
  - Manina su menu di primo e secondo livello
  - Glowing icone
  - Glow su all agents  
  - Controllare i titoli di tutti i popup
  
BUILD:
  - icone delle piattaforme al posto dei file
