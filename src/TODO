CONFIG:
  - warning se si esce senza salvare la conf
  # evidenziare gli eventi entranti quando seleziono una action

SYSTEM:
  # anonymizer non e' selezionato dopo l'edit (double-click)

OPERATIONS:
  - separare la view delle icone da quella tabellare e riabilitare il sorting sulle tabelle
  - rimuovere evidence e fs dalla visualizzazione a griglia, ed evidenziare le factory
  - il sort di "all agents" deve ordinare le factory vicine ai propri agenti (possibilmente anche dentro target)
  - infopanel scazza se si fa jump da dashboard (da target non e' selezionato l'agent resta quello vecchio)
  - mettere un tasto close sugli elementi chiudibili
  # push solo se nel target?

DASHBOARD:
  # togliere gli elementi se ricevo la push delete

RECENTS:
  # togliere gli elementi se ricevo la push delete

INJECTION:
  # lista agent solo del target selezionato

EVIDENCE:
  - modalita' di accesso al file in file open (maskera di bit da translare)
  - update dei dati sulla position (da completare)
  - visualizzazione di position multiple (da completare)


