OPERATIONS:
  - infopanel scazza se si fa jump da dashboard (da target non e' selezionato l'agent resta quello vecchio)
  - Aggiornare i searchItems dopo cancellazione (e chiusura?) di un lemento

BUILD:
  - RMI from local console

CONFIG:
  
  - aggiungere moduli che non c'erano in precedenza... (migrazione)
  

- TARGET

  - profilo/scheda target pulsante tra evidence e filesystem
   (foto, elenchi agent/evidence)
  
FILESYSTEM:
  shortcut per documenti, foto, musica
 
LOW PRIORITY:

  - le scrollbar in dashboard non devono andare sopra gli elementi
  - edit delle subaction in orizzontale (se possibile)
  
TASKS:
  - Problema cancellazione/chiusura di task
  
AGENT

- spostare config in seconda posizione nella tile list


NETWORK INJECTORS

- problema al save su new rule
- search items non filtrati

WISHLIST

//selezione multipla dei file da file system



GUIDA HTML CONTESTUALE

COMMANDS e IPS
filtro anche su received


FILESYSTEM

param default

DSK OFFLINE FILE MASK


IP solo 1 data (acquired e ridurre larghezza colonna ip)

AGENT 
purge > form: data e dimensione  

remote agent purge (arg purge)
Array 2 val data e size (numeri)
