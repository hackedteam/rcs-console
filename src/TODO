ACCOUNTING:
  - quando si crea un gruppo si fa l'autojoin di te stesso

CONFIG:
  - modifica della description

OPERATIONS:
  - infopanel scazza se si fa jump da dashboard (da target non e' selezionato l'agent resta quello vecchio)
  - inibire la creazione di target o agent se l'operation chiusa
  - inibire lo spostamento dentro cose chiuse
  - move dei target solo per admin
  - move di factory e agent solo per tech

EVIDENCE:
  - rendering print type
  - modificare label export con download
