CONFIG:
  - dropdown degli agent per target in infection
  # separare azioni mobile/desktop
  - afterinst e timer loop non hanno end (rimuovere pallino)
  - window non ha repeat e stop (rimuovere pallini)
  - winevent non ha repeat e stop (rimuovere pallini)
  - caption di default degli eventi vuota e poi caption dinamica (se non specificata)
  - descrizione delle subaction (undefined undefined)
  - mettere a tutte le date come default "oggi"
  - dateto nell'evento date
  - icona conference

DASHBOARD:
  - ordinare per sync gli elementi

DOWNLOAD MANAGER:
  - evitare che si aprano piu' istanze della finestra
  - a volte il cancellamento fallisce (il task viene rimosso ma il renderer resta appeso)

OPERATIONS:
  - separare la view delle icone da quella tabellare e riabilitare il sorting sulle tabelle
  - il sort di "all agents" deve ordinare le factory vicine ai propri agenti (possibilmente anche dentro target)
  - pulsante add to dashboard a operation, target, agent
  - rimuovere evidence e fs dalla visualizzazione a griglia, ed evidenziare le factory
  - tasto "up one level" e rendere coerente la navigazione (ogni click un nuovo elemento nel breadcrump, es: config list, config edit, etc).
  - upgrade agent dentro agent deve usare statemanager e non selected object (tipo infopanel)
  - infopanel scazza se si fa jump da dashboard
  - mettere i puntini su instance nell'infopanel
  - capitalizzazione stringhe (stati e platform) (info panel)
  - se scelgo "all agents" non compare la actionbar
  - infopanel non deve uscire dallo schermo (controllare con le localizzazioni)
  - sorting per colonne nella griglia
  - campo "uninstalled" per gli agenti "closed"
  
ALERTING:
  - jump-link alle evidence
  
FILTER:
  - in evidence e audit, inserire "last week"

FILESYSTEM:
  - implementare la vista ad albero

EVIDENCE:
  - si spacca il breadcrumb alla refresh in evidence
  - implementare filtri su date received
  - implementare filtri su agent (se in target)
  - filtri automatici dal summary
  - filtro "all" su date
  - sistemare tutti i renderer per le evidence
  - vista avanzata
  - colonne info e agent resizable
  - abilita tutti i tipi se clicco sul check (e viceversa)
  - a capo in device render
  - controllare il peer della chat
  - nome del processo in keylog

SYSTEM:
  - abbellire sezione backend

MISC:
  - rimuovere dipendenza da AlertPupUp in UserManager e EvidenceManager
  - logout infinito

ACCOUNTING:
  - tabella in accounting
  - altezza della lista dei gruppi nell'editing utente
  - menu dei gruppi non deve uscire dallo schermo (edit utente)
  - controllare la localizzazione delle tabelle sotto (group).
  
MONITOR:
  - save cancel in alerting group
  - icone di NI e ANON
  
BACKUP:
  - se faccio refresh la selezione se ne va ma restano abilitati i pulsanti di actionbar


Manina su menu di primo e secondo livello
Manina su dashboard, 1 click, mouseove su icone (tipo system)
Glowing icone
Recents 1 click
Glow su all agents
Controllare i titoli di tutti i popup
Far sparire il selettore delle azioni dopo selezione
Manina, 1 click su moduli
Popup di conferma dove servono (switch config, build config, etc..)
